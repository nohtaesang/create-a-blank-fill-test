type Question = {
	id: number;
	subjectId: number;
	order: number;
	text: string;
	selectedIndexes: number[];
};

// action
export const subject

const getSubjectList = (ownerId: number) => ({
	type: '',
	payload:
});
